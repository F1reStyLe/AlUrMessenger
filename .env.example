# Phase 1.1: only APP, LOG and HTTP settings are implemented.
# Export these variables before starting a binary; .env is not loaded implicitly.
APP_ENV=development
APP_SHUTDOWN_TIMEOUT=10s

LOG_LEVEL=info
LOG_FORMAT=json

# When unset: API uses 127.0.0.1:8080, worker probes use 127.0.0.1:8081.
# Use a different HTTP_ADDR for each process if overriding it.
# HTTP_ADDR=127.0.0.1:8080
HTTP_READ_HEADER_TIMEOUT=5s
HTTP_READ_TIMEOUT=10s
HTTP_WRITE_TIMEOUT=15s
HTTP_IDLE_TIMEOUT=60s
HTTP_MAX_HEADER_BYTES=32768
HTTP_MAX_BODY_BYTES=1048576

# JWT, POSTGRES, REDIS, KAFKA, MINIO, ENCRYPTION, CORS, RATE_LIMIT,
# UPLOAD, RETENTION and WEBHOOK settings arrive with their implementations.
# No credentials or fake connections are required by the probe-only bootstrap.
